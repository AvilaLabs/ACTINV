# Everything the CI controls may import. Anything not listed here fails controls/check_dependencies.py.
# Constants (CRAM coefficients, the 709-group structure) are vendored under data/ rather than pulled from a package.
# Versions are pinned (the set the runner resolved on 2026-09-23) so a numerical-library release cannot shift
# the floating-point controls without a reviewed change here.
numpy==2.5.3
scipy==1.18.1
maturin==1.15.0
h5py==3.16.0
