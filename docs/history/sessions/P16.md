# ACTINV P16 — typed boundaries and metamorphic assurance close, 2026-08-28

**Protocol:** `protocols/ACTINV-P16_PROTOCOL.md`
(`58d9debbb3e8892ab0ad0bf3642cba5fc1afa31ffbc1079cd26095c5d0e2ce19`).
**Verdict (`controls/check_p16.py`): P16-CONDITIONAL.** Every frozen product, scientific, compatibility, performance
and quality gate passes. The conditional suffix records one CI evidence-availability repair; no physics, public
result, tolerance or performance criterion changed.

## What changed

ACTINV now converts its compatible raw JSON/Python/Rust inputs once at the spec/core boundary into eleven distinct,
private-field `#[repr(transparent)]` scalar types: seconds, electronvolts, kelvin, grams, atoms per gram, particle
flux, dimensionless flux multiplier, particle fluence, barns, rate per barn-second and rate per second. Every scalar
has the exact size and alignment of `f64`. The production path shares one validated physical view between preparation
and execution, while the existing public raw helpers and prepared-run signatures remain available.

The barn-to-square-centimetre factor occurs at one named rate constructor. Collapsed barns and physical flux can no
longer be multiplied as unrelated scalars in reaction assembly. Six isolated downstream consumers prove that
time/energy, mass/temperature, physical-flux/multiplier, cross-section/rate, grams/atoms-per-gram and energy/rate
mix-ups are Rust type errors. Positive and legacy consumers compile, and the complete boundary inventory is recorded
in `docs/QUANTITIES.md`. P16 added no dependency, `unsafe`, clone workaround, shared mutability or duplicated boundary
allocation.

## Scientific and compatibility evidence

The independent metamorphic control executes both the signed v1.0.1 baseline and candidate through the production
path. All seven frozen relation families pass:

- source scaling and reciprocal fixed-fluence errors are exactly zero in the selected fixture, and per-gram results
  remain bit-identical when mass alone changes;
- analytic parent/daughter decay differs by at most `8.192e-16`, with atom conservation within `3.277e-16`;
- split schedules agree with unsplit decay, trace and coupled runs within `1.639e-15`;
- exact-grid copying, split-grid conservation, source scaling and one/four-thread mesh identity pass exactly;
- the trace/coupled small-burn-up difference agrees with the analytic first-order limit to `1.418e-7` relative;
- accepted duration spellings all resolve to exactly 300 seconds, while negative, nonfinite and unknown-unit inputs
  fail without publishing a result.

Five compact trace/coupled/charged/uncertainty/radiological cases, mesh output and the production example are exactly
equal between release and candidate after removing only the frozen timing field. CLI, prepared and Python entry points
share the same normalized hashes. The fixed rejection corpus retains exact status, message context and no-output
behavior. Eight independently applied evidence mutations are rejected by the checker.

## Runtime and quality

| production-example measure | signed v1.0.1 | P16 candidate | candidate / release |
|---|---:|---:|---:|
| warm median wall | 767.84 ms | 745.20 ms | 0.9705 |
| warm p95 wall | 789.02 ms | 769.98 ms | 0.9759 |
| peak RSS | 129,073,152 bytes | 129,089,536 bytes | 1.00013 |

These are alternating 15-process samples per binary after five warm-ups on the same host, with scalar thread
variables fixed to one and all four public input hashes reverified. The normalized scientific result is exact. The
candidate is about 3.0% faster at the median and 2.4% faster at p95; memory is effectively unchanged. These numbers
are regression evidence for this host and workload, not a general speed claim.

All four exact workspace Rust commands, doctests, four Python-binding Rust gates and sixteen retained product/control
checks pass—25 local quality commands in total. The source/evidence checkpoint is
`ede20289ff63951e61db536e2e36dffa5809bd62`, and canonical GitHub Actions run `33223472844` passed every workflow
step: <https://github.com/AvilaLabs/ACTINV/actions/runs/33223472844>.

The first pushed checkpoint had already passed the workspace Rust gates, release build and unit probe when its P16
step found that Actions' default depth-1 checkout did not contain the deliberately frozen v1.0.1 comparison commit.
Hashed Amendment A (`12903283e78171ddd64b07964945f935a45e09879ae701dcadbe4ea51ed99f21`) changes the checkout to full history
and nothing else. The complete replacement workflow then passed, so the frozen one-repair rule yields
`P16-CONDITIONAL`.

P16 does not authorize a version bump or registry publication. P17 may next freeze open validation and attribution;
it remains unopened until this closure commit and its workflow are complete.
