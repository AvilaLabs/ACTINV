# Archived phase sessions

These records preserve the closeout context for ACTINV phases P1 through P12. They are retained as development and
validation history; the user-facing entry points are the project README and the documentation in `docs/`.

The machine-readable gate evidence remains under `results/`, and frozen protocols remain under `protocols/`.
