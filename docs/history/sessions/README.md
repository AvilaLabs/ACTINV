# Archived phase sessions

These records preserve the closeout context for ACTINV's numbered development phases and the post-release CB1
campaign. They are retained as development and validation history; the user-facing entry points are the project
README and the documentation in `docs/`.

The machine-readable gate evidence remains under `results/`, and frozen protocols remain under `protocols/`.
