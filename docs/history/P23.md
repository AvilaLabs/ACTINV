# P23 — demand-led capabilities (feed/removal, reverse calculation, damage observables)

**Verdict: P23-PASS.** Frozen protocol `fa0df3411e7e2d1d8c5777810db03e76563d6dec1f695fb9219dc0ce7ee59dd5`,
opened at `325f207`, closed at the G5 checkpoint. Amendment A (`d03ac16b…`) records the falsified G3 premise —
no local evaluation store carries MT=444 — and substitutes a persisted synthetic ENDF mini-corpus for builder
qualification while verifying honest zero coverage on real TENDL-2025 files.

## What landed

- **G1 (`4f0d83f`)** — schedule-level `feed` (constant atoms s⁻¹ g⁻¹ per nuclide, applied on every step including
  cooling) and first-order `removal` (s⁻¹ per nuclide or per element over all its tracked states). Removed atoms
  accumulate in a dedicated sink reported as `removed_atoms_per_g`; trace-mode reservoir nuclides are exempt and
  named in the ledger; a fed reservoir nuclide carries a real removable tracked population. An absent or empty
  section is byte-identical to v1.0.1 output. 22-check battery: analytic decay-corrected feed saturation,
  first-order removal bookkeeping, dense-expm cross-checks, reservoir exemption, mesh/Python parity, and strict
  failures for element-keyed feed, unknown nuclides, negative and non-finite rates.
- **G2 (`b93b157`)** — `actinv reverse` / `actinv.reverse`: scalar weighted-least-squares multiplier recovery and
  `--segments` Lawson–Hanson NNLS over per-irradiation-step multipliers, with condition number, standard errors,
  residuals, χ²/dof, problem/measurement SHA-256, and an explicit `method_limits` statement. Named refusals for
  coupled mode, underdetermined or rank-deficient systems, zero-sensitivity columns, feed/removal schedules, and
  absent nuclides. 17-check battery recovering planted multipliers to ≲1e-12.
- **G3 (`b19e8ff`)** — `actinv build-damage` collapses ENDF-6 MF=3/MT=444 damage-energy sections through the
  build-library pipeline into a hash-pinned `actinv-damage-table-1`; the spec `damage` section folds those rows
  over composition-resolved target inventories into NRT dpa per element and in aggregate, with uncovered targets
  named and `require_complete` failing closed. 22-check battery plus a checker that re-parses the persisted corpus
  and independently re-collapses both interpolation laws at 1e-9.
- **G4** — absent-feature output reproduces the frozen G0 identity hash byte-for-byte; release wall time 1.03× and
  peak RSS 1.003× the parent on the frozen workload (bound 1.05). SPEC/METHOD/QUANTITIES/QUALIFICATION/README and
  python-README updated; worked examples for feed/removal, damage, and reverse under `examples/`.
- **G5** — `controls/check_p23.py` re-ran all four gate checkers, rehashed every committed evidence artifact,
  re-derived the feed/removal closed forms from its own ENDF decay-constant reader, re-folded a damage step from
  the committed built table, repeated the WLS arithmetic on recorded sensitivities, verified NNLS KKT optimality
  against a checker-built design matrix, re-verified the identity hash, and rejected 8/8 planted evidence
  mutations.

## Known limitations (explicit)

- Reverse is linear-regime only (trace mode); coupled mode and feed/removal schedules are named refusals.
- Feed/removal is schedule-level: no coupled flowsheet, no pathway attribution of fed inventory.
- Damage implements NRT only — no Kinchin–Pease, athermal recombination, or damage covariance; targets are the
  declared material composition, never transmutation products.
- `build-damage` needs evaluations carrying MF=3/MT=444. TENDL-2025/EAF-2010 as distributed do not; source
  `heatr`-processed or equivalent damage-energy evaluations until a distributed table exists.
